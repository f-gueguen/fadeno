# @fadeno/framework

## 0.1.0-alpha.1

### Patch Changes

- Publish the unchanged first public alpha package from a new immutable source
  after the initial registry transport refused incomplete hosted repository
  visibility evidence.

## 0.1.0-alpha.0

### Minor Changes

- 985a22f: Add the scaffolded stock-TypeScript application-test workflow with production
  renderer and handler examples, deliberate failure output, and stale-test
  recovery evidence.
- 0f3b351: Add the deterministic `fadeno create --project-root <path>` workflow with a
  tested production-package scaffold and fail-closed path ownership.
- 18dbf8f: Publish the first public alpha package with the demonstrated check, build,
  development, production start, routing, rendering, resource, action, and
  protected-session workflow.
- 0272684: Add the public immutable deployment-artifact command with production-only
  dependencies, runtime secret injection, secure health evidence, and explicit
  prior-release rollback.

### Patch Changes

- eb21ec9: Refuse malformed configuration and environment file bytes before compiler or
  build admission, and classify invalid UTF-8, invalid percent-decoded text, or
  malformed multipart action bodies as `FADENO_ACTION_BODY` before application
  code can observe replacement text.

This changelog is generated from reviewed Changesets.
