# Risk ledger

This ledger tracks current risks that can change implementation order or public
scope. It is not a feature list.

| Risk | Current control | Escalation trigger |
| --- | --- | --- |
| Browser updates destroy user state | ADR 0014 narrows structural reuse; ADR 0045 accepts only current-operation, no-store updates whose relevant preceding document and element layout are proven unaffected, with affected/unknown refusal and native current-truth recovery | Implementation applies an affected, unknown, stale, duplicate, cached, cross-generation, or misrouted update; enhancement overwrites user/browser-owned state; or uncertain mutation recovery resubmits the mutation |
| Browser loading or private decoding weakens native, CSP, or isolation authority | ADR 0047 permits only explicit startup through a generated same-origin module using the renderer-owned request nonce; the current packed gate proves wrong/missing nonce, missing artifact, JavaScript-disabled native controls, rollback, exact browser graph isolation, independently measured byte/depth/count/time limits, cancellation, cross-generation refusal, and redacted decision output | Import starts work implicitly; ordinary application markup gains a script sink; browser code becomes reachable from neutral/Node graphs; a refused module damages native controls; decoder metrics come from input; hostile bytes, identity, credentials, or markup escape into decisions/logs; or private schema becomes a public promise |
| Server update projection diverges from native authority or leaks request evidence | ADR 0048 consumes one native response once, binds exact request/response ownership by opaque object identity, records route/resource/action provenance during construction, and proves packed one-pass execution, authorization/cross-user refusal, redaction, rollback, and stale recovery | Projection reruns application or authorization work; copied, stale, unowned, cross-generation, or cross-user evidence publishes; protected markup, proof, session, owner, submitted value, or failure detail enters a record; or a mutation is repeated during recovery |
| Enhanced navigation prevents native behavior before it can preserve browser or user state | ADR 0049 permits only ordinary same-origin GET links, checks a conservative state predicate before interception and again before commit, keeps correlation separate from opaque server authority, cancels superseded work, and returns refused GET work to native destination/current truth | An ineligible or unsafe link is prevented; stale work commits; transported markup executes; browser correlation grants authorization; partial document/history state becomes visible; or rollback damages native links |
| Enhanced history exposes a URL, document, focus, or scroll from different entries | ADR 0050 requires trustworthy secure identity creation, fresh re-keying on every actual start, conservative same-document unsafe-scroll retention, a bounded exact state/URL registry in addition to active-chain/entry identity, exact selected-record recovery without URL-only consumption, guarded instance/prototype wrappers and runtime writes distinct from application state, verified manual-restoration readback, one bounded persistence URL class, commit-owned displayed identity and trusted URL, link/traversal source ownership revalidation, post-flush source refresh, actual precommit node/focus/selection retention, atomic document/history/metadata/focus/lifecycle rollback, exact startup wrapper cleanup, startup/native-departure restoration rollback, actual destination focus/scroll postconditions, monotonic combined document/element scroll recording, eligible and refused activation supersession, pending-link/traversal close cancellation without overlapping restart, destination-entry-before-viewport ordering, complete link/traversal pushed-entry rollback before native reselection or replacement, canceled-departure repair for both confirmation mechanisms and every fallback phase, pre-commit flush, newest-traversal suppression, and current-URL reload for copied, foreign-chain, malformed, unowned, or unsafe-scroll entries | Serialized private fields grant ownership without current runtime creation; a selected entry displays another entry's document; application state changes during link or traversal work; delayed recovery overwrites newer activation; close permits pending or restarted work to commit; persistence accepts a URL class its writer cannot retain or consumes an unknown entry by URL; a failed commit loses focus, selection, metadata, or original node identity, duplicates a destination, or claims scroll ownership without verified readback; a canceled reload, close, or replacement leaves selected URL and old markup paired; automatic restoration races startup, departure, recovery, close, or commit; bounded tracking forgets unsafe ownership; same-URL cleanup clears another entry; numeric scroll is restored without unchanged-layout proof; or a late traversal publishes |
| Native browser semantics diverge across enhanced activation | ADR 0050 resolves the actual current browsing context, keeps explicit referrer directives native, restores exact History instance/prototype descriptors, rejects unobserved native pushes by wrapper and write-sequence provenance, rechecks live traversal scroll including independently scrollable root elements, carries unsafe evidence through repair, and closes on persisted-page restoration failure only when no committed recovery owner remains; ADR 0052 aborts obsolete persisted-page recovery work and starts native current truth before teardown | A target resolves differently; Referer privacy changes; a later prototype wrapper is bypassed; copied state gains ownership; late/root-element scroll is forgotten; repair clears unsafe evidence; a persisted page strands committed stale markup; or traversal runs beside automatic restoration |
| Enhanced forms change controls, bypass action authority, repeat uncertain mutation, or publish stale redirect truth | ADR 0051 derives successful controls from one platform `FormData(form, submitter)`, retains GET as navigation-only, admits POST only for the exact HTTPS generated action owner, reuses the native origin/proof/replay/authorization/upload/session/redirect/revalidation path once, gives only the submitted form bounded busy state, and recovers every uncertain POST through trusted GET current truth without resubmission; ADR 0052 consumes mutation and redirect-GET results independently, bounds control/record/text/byte handoff capture before serialization, refreshes current truth instead of issuing an unobservable over-limit destination, freezes admitted control/attribute/ancestry/effective disabled inheritance/exact option/optgroup hierarchy/file/focus/caret identity, bounds post-dispatch form destinations and matches exact selected URLs, verifies exact fragment staging and rollback entry/URL provenance, catches synchronous rollback refusal, makes pending cleanup one-shot, carries recovery through enhanced, selected, persisted, unsafe-traversal, rollback, and recovery-GET supersession, starts same-resource native reload synchronously with its intended fragment delimiter, rejects stale or duplicate results, and refuses observable unenhanceable same-context departures while recovery is live | An unsupported form is intercepted outside the documented recovery refusal; browser serialization differs from final successful controls; GET reaches mutation authority; POST bypasses origin, proof, replay, authorization, or limits; handoff or observed-destination capture is unbounded; a no-document destination strands stale markup; a later `FormData` snapshot displaces the browser-selected URL; a hook substitutes staged state, rollback throws without recovery, or an unrelated popstate completes rollback; old cleanup clears a newer owner; submitted values enter evidence; a post-handoff edit, ancestry move, caret move, or same-metadata file replacement is overwritten; redirect GET reuses mutation identity or an admitted result, publishes after supersession, loses recovery during persisted restoration, traversal, rollback, or a superseded recovery GET, or leaves stale markup or the wrong URL after a direct, chained, or empty-fragment handoff; or recovery issues a second POST |
| Native recovery takeover changes form, history, or request-privacy semantics | ADR 0052 uses a preinstalled finalizer after document listeners, validates final method/action/target/origin/credentials/trust/privacy before one successful-control construction, freezes safe routing before later `formdata` microtasks, bounds distinct candidates and matches the selected complete URL, accepts image submitters only with browser-owned entries, re-reads final link ownership, refuses observable unenhanceable same-context departures before requests, uses no departure timeout, preserves browser-owned separate-context modified-primary/middle-button destinations while recovering the opener, retains explicit privacy ownership, repairs or replaces failed history state, and keeps fragment, rollback, and persisted-page recovery ownership live through teardown | A late link, method, target, or `formdata` listener is ignored; an unsafe destination constructs controls; a later serialization changes the retained destination; a structural or recovery-time edit is overwritten; elapsed time races a live native GET or POST; an unobservable cross-document activation is claimed as recovered; an initially external or separate-context activation loses opener recovery; an empty delimiter is lost; failed repair retains a staged URL; Back loses the preceding entry; a policy-protected activation causes a forced request; or cancellation, synchronous rollback failure, or persisted restoration failure leaves stale committed truth |
| Handler extraction is too magical or too narrow | ADR 0015 accepts only the bounded corpus and conservative refusal; implementation stays V3 | Useful interactions require hidden hydration or broader capture than the accepted contract |
| Security semantics drift behind runtime work | Each trust boundary ships with threat and negative tests | A boundary is implemented without size, origin, authorization, and logging behavior |
| External decoder failures escape classification or resource bounds | The A0-09 threat inventory and fixed-seed `check:a0-decoder-fuzz` gate exercise fourteen exact production surfaces twice with accepted/refused controls, declared per-surface input bounds, a child-process deadline, complete classifications, deterministic digests, and an action-response secret canary | A supported public input reaches an unreviewed parser, produces an internal failure instead of a stable refusal, exceeds its declared bounds, changes between identical replays, or leaks submitted bytes |
| Native action replay or protected-session identity crosses its supported owner | ADR 0035 binds bounded single-use proofs to action/route/generation/session, selects exact-origin POST, encrypted host-only cookies, absolute expiry and privilege rotation, and explicitly limits V1 action serving to one process; V1-13 integrates that owner through focused public-runtime and packed three-browser evidence | A replay entry or session decision crosses processes without an atomic owner; a stolen credential needs immediate revocation; an action runs before complete boundary acceptance; or a later change weakens cleanup, redaction, key, expiry, origin, redirect, or rotation evidence |
| Revalidation regresses beyond its accepted cost | ADR 0020 and the immutable H4 workload baseline remain regression gates; V1-11 reruns every active dependency and proves removing unsafe `keeps` removes no correctness read | Runtime integration exceeds locked latency/query/memory controls or makes correctness depend on `keeps` |
| Resource caching crosses request or authorization ownership | ADR 0034 permits only a fresh request-owned promise map; V1-11 implements bounded framework-owned structural input identity, response-lifecycle cleanup, cross-authorization packed evidence, failure recovery, cancellation, and refusal of every cross-request result-cache policy | A result, failure, key, or dependency survives request completion; authorization or representation affects a value without request isolation; or V1 adds shared caching without a superseding decision and isolation proof |
| Documentation contradicts executable behavior | One authority order, generated guides, and the V1-14C current-packed documented-command replay | A public example or spec cannot be derived from tested source, or a documented command succeeds only through workspace/private state |
| The canonical demonstration overstates private or unobserved behavior | V2-05A permits only application-owned public facts and named reproducible qualification evidence in the evaluator-facing request thread; V2-07A integrates accepted enhanced outcomes through the same public-behavior boundary, proves setup from a clean packed application in three engines, keeps redirected server documents and dirty-control refusal explicit, and exposes only tracked application source in its developer panel; private analyzer, build, security, transport, and browser-session records are not presented as live request telemetry | The UI fabricates an execution count, authorization result, timing, browser decision, build guarantee, or private schema; a visible claim lacks a current-packed assertion; setup depends silently on workspace state; or the demonstration presents a pre-V2-08 refusal as supported reconciliation |
| Automated accessibility evidence is mistaken for complete qualification | V1-14D tests the engine-portable native semantic, focus, activation, label, and error-association baseline with JavaScript disabled and records the remaining manual/assistive-technology work explicitly | An automated browser check is presented as proof of focus visibility, user-preference behavior, or assistive-technology usability |
| Independent usability evidence is fabricated, coached, selected, or privacy-invasive | ADR 0043 records the first alpha as `deferred-unqualified` and preserves ADR 0042's frozen tasks, two-non-contributor minimum, exact-artifact identity, all-attempt retention, bounded anonymous records, and synthetic refusal for later collection; A0-07B1/B2 retain replay, reconstruction, privacy, and closed-file-set controls | A release treats unavailable attempts or automation as user success; omits failed attempts; changes tasks after observation; treats facilitator help as independent success; lacks exact artifact identity; retains unreferenced bytes; or retains names, contact details, secrets, paths, or unrelated command history |
| Package surface grows before consumers exist | Public packages require an ADR and demonstrated consumer; ADR 0046 accepts one `./browser` facade, and ADR 0047 fixes its minimum explicit symbols only after disposable and current-packed consumers prove topology, execution, fallback, and one pending minor Changeset | A package or subpath exists only to mirror an internal diagram, browser code becomes reachable from `.`, `./node`, or server/compiler graphs, a second browser version owner appears, or implementation changes the package without reviewed release intent and executable consumer evidence |
| Scope exceeds maintainable capacity | Outcome gates precede tooling and adapter breadth | Work begins on optional tooling before the first vertical slice passes |
| Registry or publication identity drifts | ADR 0037 selects `@fadeno/framework`, the exact export/bin map, public-source provenance, and trusted-publisher identity; ADR 0044 reconciles the registry-mandated `latest` alias with the intended `alpha` channel, requires both to resolve to the exact first alpha, records zero active tokens and no hosted bootstrap secret after maintainer-owned cleanup, and binds later transport to the exact trusted publisher; V2-11A must accept the later-alpha alias policy and V2-11B must separate version-aware current gates from immutable first-alpha evidence before V2-12 publication | Package/source/workflow identity diverges, historical alpha.1 proof is rewritten as current state, either alias moves independently without the accepted later-alpha policy, another channel appears, provenance changes, a credential survives bootstrap, the trusted publisher drifts, or publication occurs before the accepted release boundary |
| Styling scope expands before application evidence exists | ADR 0036 limits alpha to application-owned same-origin external CSS, retains inline-style refusals, and defers compiler and asset-pipeline ownership | Native CSS cannot express a demonstrated application requirement, or styling work begins to require framework ordering, generated identity, source maps, or diagnostics |
| Project creation escapes its declared root or drifts from production | ADR 0039 requires a missing target, symlink-free parent path, exclusive target-directory claim, fixed contained allowlist, rollback, exact executing-package version, and the existing compiler/runtime; the packed generated consumer passes check/build/dev/start from the current tarball | A scaffold overwrites existing work, traverses or follows a link, leaves an accepted partial target, contacts a network, or passes without the packed generated consumer |
| Application tests drift into a second runtime or stale output | ADR 0040 uses stock TypeScript, the platform runner, production public runtime surfaces, one exact emitted test entry, and a cleaned disposable output tree; no helper or package surface is added | A test reimplements framework behavior, imports private code, enters the production artifact, executes deleted output, or needs framework-only test semantics without a reviewed contract |
| Deployment artifacts mix source, secrets, development packages, or releases | ADR 0041 accepts only a missing immutable output outside the project, installs the exact production closure with lifecycle scripts disabled, reduces the root to `dist`, `node_modules`, and a runtime manifest, injects origin/session secrets only at start, and rolls back by restarting an unchanged prior directory | A release contains source, tests, environment files, a project development dependency, a lifecycle side effect, a mutable active pointer, mixed build/runtime identities, a non-loopback listener, or a failed candidate damages prior bytes |
| Analyzer consumers observe stale or mixed generations | ADR 0030 requires versioned documents, workspace epochs, complete affected closure, atomic replacement, cancellation, and stale-result suppression; B7D1-B7D3 serialize retained work through batched analysis, provisional application, compiler validation, final freshness and commit/rollback, and grant derived ownership only to the newest complete generation | Diagnostics, declarations, manifests, mappings, or deletions from different epochs become jointly observable |
| Generated route replacement loses the last accepted disk set | B7C applies only an exact current diagnostic-free publication; B7D3 retains the previous or unchanged generation, uses atomic renames before destructive cleanup, distinguishes non-authoritative garbage, fault-tests partial removal, and keeps unresolved restore or cleanup state recoverable in-session and on restart | Diagnostic refusal deletes disk output, partial cleanup damages the rollback copy, mixed generations appear, or recovery guesses ownership of transaction debris |
| Complete project analysis scales superlinearly before feedback qualification | Existing document synchronization, diagnostic authority matching, deep-route ancestor validation, and deterministic graph topology/affected queues retain correctness-first full-state work; the graph queues currently re-sort arrays and are quadratic or worse on dense accepted graphs. C5B's recorded host baseline is 49.823042 ms diagnostic median/53.108291 ms p95 and 448.635208 ms cleared median/450.980584 ms p95. These values establish current scale only; optimize in a later bounded slice if canonical growth triggers the risk | Canonical application growth or dense graphs make complete edit-to-visible or edit-to-cleared latency operationally material; a future accepted budget is missed |
| Route matching cost grows with the complete route set | The generated matcher preserves accepted precedence by scanning the deterministic route set; no route-scale performance claim exists. Introduce a separately qualified generated decision structure only if canonical route growth triggers the risk | Route count makes matching a material share of server latency or fails a later locked request budget |
| Analyzer growth hardens one rigid schema | Module-owned facets are namespaced, independently versioned, bounded, and explicit about unknown/newer versions | A new framework module requires central enumeration or an unsupported facet is silently interpreted |
| Static evidence is presented as observed runtime truth | Static analyzer and runtime records remain separate facets linked only by evidence identity | Source analysis claims request ordering, authorization, streaming, cancellation, or browser outcomes |
| Development lifecycle appears correct only in direct tests | V1-DX-C1 integrates overlays with the same project authority; C2 qualifies current-packed document/diagnostic replacement; C3 observes only adapter deliveries while proving genuine imported transitivity, configuration epochs, all generated bytes, desired-refusal versus last-good state, rename/deletion cleanup, and recovery; C4 proves deterministic interruption, newest-only delivery, and exact private ownership cleanup; C5 measures the complete loop | Stale errors survive repair, transitive artifacts lag, obsolete work publishes, or the harness passes by reading internal current state instead of observing delivery |
| Unsaved lifecycle evidence bypasses canonical project policy | C1 adds one overlay-aware private project operation boundary before the disposable consumer exists. One immutable capture supplies configuration, existing route-source bytes, diagnostics, provenance, and artifacts; overlay generations are analysis-only, new/missing roles and unrelated files refuse, and the whole ordered edit batch causes one recomputation | A lifecycle fixture constructs a parallel raw analyzer policy, writes an unsaved buffer to disk, mixes saved and overlay generations, applies overlay-derived artifacts, admits a generic virtual filesystem, or permits managed overlays to diverge from their declared owner |
| Feedback evidence is selected, sensitive, or attributed to stale packed output | C5A binds the workload and raw verifier. Review found that `a1` and `a3` fingerprinted environment values, so their raw identities and timings were removed before merge and only checked redaction tombstones remain. C5B accepts `a4` only after reconstructing ancestor source `4d57a69` with Git-owned modes, performing a frozen offline build/pack/extract, independently comparing tarball/installed/runtime/compiler identities, checking name-only environment context, and verifying all raw events/artifacts/disk/cleanup, non-sensitive host context, derived summary, and manifest. Captures publish the complete verifier-ready file set atomically; every declared sample contributes and no timing-based retry selection or budget exists | A result cannot prove which bytes ran, depends on extraction umask or unreachable history, trusts coordinated sibling digests, retains a value-derived environment fingerprint, emits partial evidence, omits invalid attempts, selects a favorable retry, or presents baseline timing as an incremental bound |
| Filesystem notifications are mistaken for semantic truth | B7D4 admits only contained private invalidation hints, separates raw-notification and accepted-admission identity, bounds retained paths by count and UTF-8 bytes, excludes owned output and repository metadata, converts rename/missing-name/alias/overflow ambiguity into full authoritative rescans, coalesces with bounded debounce and maximum delay, and retains one later batch for work arriving during refresh | Rename/null/duplicate/overflow events miss a change, publish twice, escape containment, retain unbounded notification input, or trigger an output loop |
| Analyzer, compiler, and server expose different generations | B7D3 serializes current analysis, provisional B7C application, stock input discovery and validation, exact installed-package mapping and manifest identity, post-validation freshness, and acceptance or rollback; B7D6 additionally excludes concurrent builds with crash-recoverable ownership, requires a matching independent final generation, validates newly added source and external compiler inputs, binds emitted imports and startup to the bounded declared production-dependency graph, accepts rollback only from an exact manifest-owned current-framework generation, and verifies output/runtime identity before production imports; B7D7 serves only a fully accepted development generation in a fresh verified child | Compiler/server reads during route replacement, a concurrent or crashed builder leaves ambiguous ownership, an external, remapped, added, or changed dependency input escapes freshness, an emitted development-only import escapes the production graph, fabricated rollback is trusted, stale compiler output publishes, or a request observes mixed ownership |
| Development reload leaks or serves mixed application generations | ADR 0033 keeps application imports out of the supervisor, freezes environment and runtime closure per generation, requires loader-before-bootstrap startup in isolated children, preserves last-good on candidate failure, and bounds switch, rollback, drain, repeated signal, and forced cleanup; B7D7's packed operating-system lifecycle proves direct/transitive/configuration/rename/deletion/burst rescans, diagnostic recovery, occupied-address refusal, graceful close, and repeated-signal cleanup | A failed candidate stops the accepted child, imports cross generations, changes supervisor environment, publishes mixed output, or leaves a watcher/compiler/server child after close |
| Retained analysis accumulates historical route-root source text | B7D2 atomically reconciles the full desired/forgotten owner set as saved records, requires exact managed saved revision, refuses open desired/forgotten buffers, and proves three still-existing disjoint roots retain only current source text | Repeated disjoint route-root changes make retained memory or reconciliation cost depend on cumulative historical files rather than current ownership |
