# V1 detailed plan — secure native vertical slice

V1 delivers one authenticated CRUD application whose essential behavior works
with JavaScript disabled. This plan is dependency ordered. It does not select
public syntax before the named decision gate resolves it.

## Entry conditions

- K0-11 is merged and ADRs 0014, 0015, 0018, 0020, and 0021 are effective.
- All four experiment registry entries are `qualified` and their immutable
  evidence checks pass locally.
- The hypothesis ledger contains no active K0 claim.
- The unresolved DG-V1 gates in the decision ledger remain the only gates that
  can block their named V1 contracts; ADRs 0022 through 0024 have resolved the
  toolchain, initial-adapter, and package-boundary gates.

## Delivery checkpoints

- **Integration smoke checkpoint — V1-04:** a clean private consumer installs
  the selected package boundary and drives the adapter's Web-standard request
  contract. It proves package, install, build, and adapter boundaries without
  inventing route or renderer APIs.
- **First running framework checkpoint — V1-09:** the example serves routed,
  contextually escaped, streamed HTML through public entrypoints after all
  affected V1 contracts and the private analyzer architecture are accepted. It
  has no resource/action CRUD yet.
- **Usable V1 checkpoint — V1-13:** the authenticated CRUD example routes,
  reads, submits native forms, validates, redirects, revalidates, and handles
  expected failures without JavaScript.
- **V1 exit — V1-14:** examples, reference documentation, threat coverage,
  clean-machine workflow, and conformance suites are complete and green.

## Atomic work packages

| Slice | One reviewable outcome | Features | Resolves / depends on | Required artifacts | Validation |
| --- | --- | --- | --- | --- | --- |
| V1-01 | Freeze the supported config and command workflow from a clean-machine prototype | BUILD-01, CLI-01, DX-01 | ADR 0022; K0-11 | Toolchain ADR, config/command spec, private prototype transcript, failure fixtures | Frozen install; prototype dev/check/build twice; invalid-config and precedence tests |
| V1-02 | Select one Web-standard runtime adapter contract from a private feasibility harness | ADP-01, WEB-03, TEST-01 | ADR 0023; V1-01 | Adapter ADR, capability declaration, `Request`/`Response`/stream/cancel/shutdown harness | Adapter feasibility suite on the selected runtime |
| V1-03 | Freeze the smallest package boundary using the workflow, adapter, K0 imports, and one demonstrated consumer | GOV-01, BUILD-01, ADP-01 | ADR 0024; V1-01, V1-02 | Package-boundary ADR, dependency rule, executable private consumer prototype, rollback note | Prototype import/run proof; boundary mutation tests; no deep/cross-package relative imports |
| V1-04 | Create the selected package boundary and a clean adapter smoke consumer without route/render APIs | BUILD-01, ADP-01, DOC-01 | V1-03 | Public adapter contract, private server core, executable request/response smoke consumer, install/run README | Clean checkout install/build; adapter request/response assertion; package-content check |
| V1-05 | Freeze route filesystem and generated-link contracts | WEB-01, TYPE-01 | Route decision resolved by ADR 0027 (superseding ADR 0026); V1-04, ADR 0018 | Routing ADR, collision grammar, route manifest schema, valid/invalid route fixtures | Deterministic discovery and stock-TypeScript route/link fixture suite |
| V1-06 | Implement deterministic routing and clean declaration generation | WEB-01, TYPE-01, DX-01 | V1-05 | Router, manifest generator, declaration generator, diagnostics, nested/error/raw fixtures | Two byte-identical clean generations; `tsc`; runtime route matrix; no incremental-speed claim |
| V1-07 | Freeze contextual rendering and unsafe-capability security contracts | WEB-02, SEC-01 | Rendering decision resolved by ADR 0028; V1-04 | Rendering-security ADR, threat model, raw-HTML capability, nonce/escaping fixtures | Contextual XSS, URL/attribute, raw HTML, CSP, and secret-leak negative suite |
| V1-08 | Freeze stream, boundary, timeout, cancellation, and response-commit behavior | WEB-03, ADP-01 | Resolved by ADR 0029; V1-02, V1-07 | Streaming ADR, state machine, nested failure/timeout/disconnect fixtures | Chunk, nested boundary, pre/post-commit error, cancellation, and disconnect suite |
| V1-09 | Implement the server renderer, streaming page outcomes, and first running routed example | WEB-01, WEB-02, WEB-03, ACCESS-01, DOC-01 | V1-06, V1-07, V1-08, V1-DX-A | JSX renderer, metadata, boundaries, stream integration, executable routed hello example | Clean install/build/start; HTTP HTML assertion; renderer/security/stream/adapter integration and semantic HTML checks |
| V1-10 | Freeze resource identity, request-cache, failure, and cross-request opt-in rules | DATA-01, DATA-03, SEC-01 | Resolves DG-V1-04; V1-09, V1-DX-B, ADR 0020 | Resource ADR, identity/cache spec, isolation and error-cache fixtures | Equivalent/distinct input, tenant/auth isolation, failure, unsafe-`keeps` tests |
| V1-11 | Implement request-scoped resources and correctness-first revalidation | DATA-01, DATA-03, STATE-01 | V1-10 | Resource runtime, dependency recording, request deduplication, revalidation integration | K0 H4 controls plus runtime concurrency, failure, and memory regression suite |
| V1-12 | Freeze native form, action, session, replay, redirect, and upload contracts | DATA-02, STATE-01, SEC-01 | Resolves DG-V1-05; V1-11 | Form/action/session ADR, threat model, field/file/cookie/key limits, native fixtures | Origin/CSRF, auth, replay, redirect, cookie rotation, upload and redaction suite |
| V1-13 | Implement the authenticated no-JavaScript CRUD vertical slice | WEB-01, DATA-01, DATA-02, DATA-03, STATE-01, SEC-01 | V1-06, V1-09, V1-11, V1-12 | Typed forms/actions, sessions, redirects, validation/failure pages, complete CRUD example | Browser-disabled end-to-end CRUD, type, security, adapter, revalidation and accessibility suite |
| V1-14 | Make V1 independently usable and documented | TEST-01, DOC-01, BUILD-01, CLI-01, DX-01, PERF-01 | V1-13, V1-DX-C | Tested examples, generated API reference, tutorials, diagnostics guide, migration seed, package/readme audit | `pnpm ci:local`; clean-machine example replay; docs/link/snippet checks; public-surface snapshot |

## Cross-cutting V1/DX milestones

These milestones preserve the numbered V1 plan. V1-DX-A is one architectural
slice. V1-DX-B and V1-DX-C are dependency gates whose implementation must be
decomposed into independently reversible, one-outcome sub-slices in the roadmap
ledger before code begins.

| Milestone | One owned outcome | Features | Depends on | Required artifacts | Exact validation | Delivery boundary |
| --- | --- | --- | --- | --- | --- | --- |
| V1-DX-A | Accept and schedule one private, tool-neutral analyzer architecture without exposing a product or schema | GOV-01, TYPE-01, BUILD-01, TEST-01, DX-01, DOC-01, PERF-01 | V1-08 | Analyzer ADR; aligned roadmap, specification, scope, traceability, risk, deferral, and conformance text; enforced project model | `pnpm check:docs`; `pnpm check:decisions`; `pnpm check:ledgers`; `pnpm check:model`; `pnpm check:policies`; `pnpm check`; `git diff --check` | One planning PR; no analyzer code, package export, public schema, editor manifest, or example claim |
| V1-DX-B | Establish the private analyzer foundation against the first running V1 application | TYPE-01, BUILD-01, TEST-01, DX-01, DOC-01 | V1-DX-A, V1-09 | Versioned session and document synchronization; deterministic closure and atomic publication; cancellation and stale suppression; provenance; structured diagnostics and corrections; serialization; lazy bounded static explanation | `pnpm check:v1-analyzer`; `pnpm check:v1-analyzer-package`; `pnpm check` | Multiple atomic implementation PRs declared in the roadmap ledger; each user-observable outcome adds permanent executable success, refusal, flow, and recovery evidence where applicable |
| V1-DX-C | Qualify complete analyzer and disposable editor lifecycle evidence before V1 exit | BUILD-01, TEST-01, DX-01, DOC-01, PERF-01 | V1-DX-B, V1-13 | Packed canonical-app scenarios; headless lifecycle client; stale diagnostic/artifact recovery; full edit-to-visible and edit-to-cleared measurements | `pnpm check:v1-analyzer-lifecycle`; `pnpm check:v1-analyzer-feedback`; `pnpm ci:local` | Multiple atomic evidence PRs declared in the roadmap ledger; no supported editor product or public analyzer schema |

V1-DX-B conformance is complete only when its atomic sub-slices cover saved and
unsaved synchronization, sequential edit batches, replacement, invalid version
refusal, close/reopen, normalization, direct and three-level transitive
recomputation, cycles, deletion, rename, configuration epochs, one-epoch
artifact and diagnostic replacement, cancellation, supersession, exact and
explicitly unknown ranges, construction-time provenance, causal diagnostics,
corrections, versioned round trips, and disabled/semantic/deep/truncated
explanation with redaction and malicious-contribution refusal.

### V1-DX-B atomic implementation sequence

| Sub-slice | One reversible outcome | Features | Depends on | Required artifacts | Exact validation | User-observable example boundary |
| --- | --- | --- | --- | --- | --- | --- |
| V1-DX-B1 | Establish one private versioned analyzer session and exact saved/unsaved document synchronization | BUILD-01, TEST-01, DX-01 | V1-09, V1-DX-A | Internal session/document state; versioned operation envelope; deeply immutable document-only snapshots; sequential edit, replacement, close/reopen, URI/path, text-preservation, and refusal fixtures | `pnpm check:v1-analyzer`; `pnpm check:types`; `pnpm check` | No public consumer or user-observable output; permanent conformance fixtures only |
| V1-DX-B2 | Add namespaced facet snapshots and lossless versioned serialization | TYPE-01, TEST-01, DX-01 | V1-DX-B1 | Independent facet versions; absent/unknown/newer handling; bounded contributions; lossless round-trip fixtures layered on B1 snapshots | `pnpm check:v1-analyzer`; `pnpm check:types`; `pnpm check` | Private normalized snapshot fixtures; no external schema claim |
| V1-DX-B3 | Implement deterministic affected closure, recomputation, deletion, cycles, and construction-time provenance | TYPE-01, BUILD-01, TEST-01, DX-01 | V1-DX-B2 | Dependency ownership graph; invalidation reasons; three-level recomputation; deletion/rename/configuration epochs; provenance fixtures | `pnpm check:v1-analyzer`; `pnpm check:v1-analyzer-package`; `pnpm check` | Extend the canonical app corpus when framework facts become observable |
| V1-DX-B4 | Publish one atomic epoch and suppress cancelled, superseded, or stale work | BUILD-01, TEST-01, DX-01 | V1-DX-B3 | Atomic artifact/diagnostic/deletion replacement; cancellation; ownership/version freshness; stale-result fixtures | `pnpm check:v1-analyzer`; `pnpm check:v1-analyzer-package`; `pnpm check` | Permanent recovery scenario proving stale diagnostics and artifacts disappear |
| V1-DX-B5 | Produce structured diagnostics and analyzer-owned correction intents | TEST-01, DX-01, DOC-01 | V1-DX-B4 | Parameters, phases, exact/null ranges, causal/skipped relations, internal identities, correction edits/safety, serialization fixtures | `pnpm check:v1-analyzer`; `pnpm check:v1-analyzer-package`; `pnpm check` | Permanent private canonical-app success, refusal, human/machine diagnostic, correction before/after, and recovery evidence; package inclusion and inaccessibility proof; packed public-workflow execution remains B7 |
| V1-DX-B6 | Add lazy bounded static plan/explain facets without runtime prediction | TEST-01, DX-01, DOC-01 | V1-DX-B5 | Disabled/semantic/deep/truncated/cancelled/redacted explain; malicious-contribution refusal; static/runtime record separation | `pnpm check:v1-analyzer`; `pnpm check:v1-analyzer-package`; `pnpm check` | Permanent private analyzer-produced canonical-app flow inspection showing decisions, causes, ownership, skipped work, and static outcome; package inclusion and inaccessibility proof; packed public-workflow execution remains B7 |
| V1-DX-B7A | Establish one real private project authority and pure route artifact plan | BUILD-01, TEST-01, DX-01, DOC-01 | V1-DX-B6 | Shared workspace/config resolution; real route definitions, diagnostics, explanations, and complete generated bytes; no filesystem publication or public surface | `pnpm check:v1-analyzer`; `pnpm check:v1-analyzer-package`; `pnpm check` | Private canonical-app success, collision, correction, flow, and recovery evidence derived from one analyzer operation |
| V1-DX-B7B | Execute the packed public project-check workflow through the private analyzer authority | BUILD-01, TEST-01, DX-01, DOC-01 | V1-DX-B7A | Accepted `fadeno check` executable; explicit project root, exit status, and human diagnostics; clean installed consumer with stale-package canary | `pnpm check:v1-analyzer`; `pnpm check:v1-analyzer-workflow`; `pnpm check:v1-analyzer-package`; `pnpm check` | Packed canonical success/failure/flow/recovery scenarios use only the installed executable and declared package entrypoints |
| V1-DX-B7C | Apply only analyzer-published artifacts through one transactional filesystem boundary | BUILD-01, TEST-01, DX-01 | V1-DX-B7B | Atomic replacement, unchanged-byte avoidance, deletion, freshness, containment, symlink refusal, and injected rollback | `pnpm check:v1-analyzer`; `pnpm check:v1-analyzer-workflow`; `pnpm check:v1-analyzer-package`; `pnpm check` | Running example and project integration tests no longer invoke private generation policy directly |
| V1-DX-B7D | Route build/watch project consumers through one retained analyzer session | BUILD-01, TEST-01, DX-01, DOC-01 | V1-DX-B7C | Build and development consumers; direct/transitive/configuration changes; cancellation and complete-epoch disk publication; no duplicate project policy | `pnpm check:v1-analyzer`; `pnpm check:v1-analyzer-workflow`; `pnpm check:v1-analyzer-package`; `pnpm check`; `pnpm ci:local` | Applicable canonical analyzer examples execute through public package entrypoints; unit owners remain directly testable |

V1-DX-C uses the evolving V1 application and an isolated scenario harness. It
must execute successful, failing, correction, flow-inspection, and recovery
paths from a current packed framework through public entrypoints; normalize
unstable values; assert behavior; and source documentation snippets and outputs
from verified files. Its disposable client exercises initialize, open, edit,
diagnose, apply multiple position-dependent edits, direct and transitive
changes, regeneration, configuration reload, repair, supersession,
cancellation, close/reopen, and cleanup. The feedback workload measures the
complete edit/save-to-fresh and edit/save-to-cleared path, including
invalidation, generation, TypeScript refresh, Fadeno analysis, and final
consumer-visible state. It makes no bounded incremental-performance claim.

## H3 incremental-generation constraint

V1 may regenerate deterministic declarations cleanly and avoid rewriting
unchanged bytes. It must not claim bounded single-route incremental performance
from the K0 candidate. A later optimization slice needs a new frozen contract
and must preserve correlation, stale-output deletion, determinism, and stock-tool
behavior while meeting the locked incremental/clean target.

## V1 exit

V1 completes only when V1-13's JavaScript-disabled authenticated CRUD example
and V1-DX-C's lifecycle evidence plus V1-14's independent-user workflow pass
from a clean checkout; every V1
decision gate is resolved by an effective ADR; examples import only public
entrypoints; and current scope, specifications, traceability, diagnostics,
security evidence, and package contents agree.
