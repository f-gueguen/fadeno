# V1 detailed plan — secure native vertical slice

V1 delivers one authenticated CRUD application whose essential behavior works
with JavaScript disabled. This plan is dependency ordered. It does not select
public syntax before the named decision gate resolves it.

## Entry conditions

- K0-11 is merged and ADRs 0014, 0015, 0018, 0020, and 0021 are effective.
- All four experiment registry entries are `qualified` and their immutable
  evidence checks pass locally.
- The hypothesis ledger contains no active K0 claim.
- The unresolved DG-V1 gates in the decision ledger remain the only gates that
  can block their named V1 contracts; ADRs 0022 through 0024 have resolved the
  toolchain, initial-adapter, and package-boundary gates.

## Delivery checkpoints

- **Integration smoke checkpoint — V1-04:** a clean private consumer installs
  the selected package boundary and drives the adapter's Web-standard request
  contract. It proves package, install, build, and adapter boundaries without
  inventing route or renderer APIs.
- **First running framework checkpoint — V1-09:** the example serves routed,
  contextually escaped, streamed HTML through public entrypoints after all
  affected V1 contracts are accepted. It has no resource/action CRUD yet.
- **Usable V1 checkpoint — V1-13:** the authenticated CRUD example routes,
  reads, submits native forms, validates, redirects, revalidates, and handles
  expected failures without JavaScript.
- **V1 exit — V1-14:** examples, reference documentation, threat coverage,
  clean-machine workflow, and conformance suites are complete and green.

## Atomic work packages

| Slice | One reviewable outcome | Features | Resolves / depends on | Required artifacts | Validation |
| --- | --- | --- | --- | --- | --- |
| V1-01 | Freeze the supported config and command workflow from a clean-machine prototype | BUILD-01, CLI-01, DX-01 | ADR 0022; K0-11 | Toolchain ADR, config/command spec, private prototype transcript, failure fixtures | Frozen install; prototype dev/check/build twice; invalid-config and precedence tests |
| V1-02 | Select one Web-standard runtime adapter contract from a private feasibility harness | ADP-01, WEB-03, TEST-01 | ADR 0023; V1-01 | Adapter ADR, capability declaration, `Request`/`Response`/stream/cancel/shutdown harness | Adapter feasibility suite on the selected runtime |
| V1-03 | Freeze the smallest package boundary using the workflow, adapter, K0 imports, and one demonstrated consumer | GOV-01, BUILD-01, ADP-01 | ADR 0024; V1-01, V1-02 | Package-boundary ADR, dependency rule, executable private consumer prototype, rollback note | Prototype import/run proof; boundary mutation tests; no deep/cross-package relative imports |
| V1-04 | Create the selected package boundary and a clean adapter smoke consumer without route/render APIs | BUILD-01, ADP-01, DOC-01 | V1-03 | Public adapter contract, private server core, executable request/response smoke consumer, install/run README | Clean checkout install/build; adapter request/response assertion; package-content check |
| V1-05 | Freeze route filesystem and generated-link contracts | WEB-01, TYPE-01 | Route decision resolved by ADR 0027 (superseding ADR 0026); V1-04, ADR 0018 | Routing ADR, collision grammar, route manifest schema, valid/invalid route fixtures | Deterministic discovery and stock-TypeScript route/link fixture suite |
| V1-06 | Implement deterministic routing and clean declaration generation | WEB-01, TYPE-01, DX-01 | V1-05 | Router, manifest generator, declaration generator, diagnostics, nested/error/raw fixtures | Two byte-identical clean generations; `tsc`; runtime route matrix; no incremental-speed claim |
| V1-07 | Freeze contextual rendering and unsafe-capability security contracts | WEB-02, SEC-01 | Rendering decision resolved by ADR 0028; V1-04 | Rendering-security ADR, threat model, raw-HTML capability, nonce/escaping fixtures | Contextual XSS, URL/attribute, raw HTML, CSP, and secret-leak negative suite |
| V1-08 | Freeze stream, boundary, timeout, cancellation, and response-commit behavior | WEB-03, ADP-01 | Resolved by ADR 0029; V1-02, V1-07 | Streaming ADR, state machine, nested failure/timeout/disconnect fixtures | Chunk, nested boundary, pre/post-commit error, cancellation, and disconnect suite |
| V1-09 | Implement the server renderer, streaming page outcomes, and first running routed example | WEB-01, WEB-02, WEB-03, ACCESS-01, DOC-01 | V1-06, V1-07, V1-08 | JSX renderer, metadata, boundaries, stream integration, executable routed hello example | Clean install/build/start; HTTP HTML assertion; renderer/security/stream/adapter integration and semantic HTML checks |
| V1-10 | Freeze resource identity, request-cache, failure, and cross-request opt-in rules | DATA-01, DATA-03, SEC-01 | Resolves DG-V1-04; V1-09, ADR 0020 | Resource ADR, identity/cache spec, isolation and error-cache fixtures | Equivalent/distinct input, tenant/auth isolation, failure, unsafe-`keeps` tests |
| V1-11 | Implement request-scoped resources and correctness-first revalidation | DATA-01, DATA-03, STATE-01 | V1-10 | Resource runtime, dependency recording, request deduplication, revalidation integration | K0 H4 controls plus runtime concurrency, failure, and memory regression suite |
| V1-12 | Freeze native form, action, session, replay, redirect, and upload contracts | DATA-02, STATE-01, SEC-01 | Resolves DG-V1-05; V1-11 | Form/action/session ADR, threat model, field/file/cookie/key limits, native fixtures | Origin/CSRF, auth, replay, redirect, cookie rotation, upload and redaction suite |
| V1-13 | Implement the authenticated no-JavaScript CRUD vertical slice | WEB-01, DATA-01, DATA-02, DATA-03, STATE-01, SEC-01 | V1-06, V1-09, V1-11, V1-12 | Typed forms/actions, sessions, redirects, validation/failure pages, complete CRUD example | Browser-disabled end-to-end CRUD, type, security, adapter, revalidation and accessibility suite |
| V1-14 | Make V1 independently usable and documented | TEST-01, DOC-01, BUILD-01, CLI-01, DX-01, PERF-01 | V1-13 | Tested examples, generated API reference, tutorials, diagnostics guide, migration seed, package/readme audit | `pnpm ci:local`; clean-machine example replay; docs/link/snippet checks; public-surface snapshot |

## H3 incremental-generation constraint

V1 may regenerate deterministic declarations cleanly and avoid rewriting
unchanged bytes. It must not claim bounded single-route incremental performance
from the K0 candidate. A later optimization slice needs a new frozen contract
and must preserve correlation, stale-output deletion, determinism, and stock-tool
behavior while meeting the locked incremental/clean target.

## V1 exit

V1 completes only when V1-13's JavaScript-disabled authenticated CRUD example
and V1-14's independent-user workflow pass from a clean checkout; every V1
decision gate is resolved by an effective ADR; examples import only public
entrypoints; and current scope, specifications, traceability, diagnostics,
security evidence, and package contents agree.
