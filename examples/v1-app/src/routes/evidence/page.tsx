import type { Page } from "@fadeno/framework";

const page: Page = () => (
  <div class="lab-page">
    <header class="lab-heading">
      <p class="eyebrow">Qualification evidence</p>
      <h1>Reproduce the claims outside the UI.</h1>
      <p>Build integrity and browser lifecycle guarantees are not ordinary request facts. This page labels them honestly and gives the exact checked command for each evidence set.</p>
    </header>
    <div class="evidence-list">
      <section class="evidence-row"><div><span class="status-chip status-success">Executable</span><h2>Routes, resources, failures, HTTPS actions</h2><p>Packed public package, three browsers, deliberate failure, correction, flow, and stale-state recovery.</p></div><code>pnpm check:v1-running-example</code></section>
      <section class="evidence-row"><div><span class="status-chip status-success">Executable</span><h2>Development lifecycle</h2><p>Direct and transitive reloads, last-good diagnostics, cleanup, and graceful shutdown.</p></div><code>pnpm check:v1-development</code></section>
      <section class="evidence-row"><div><span class="status-chip status-success">Executable</span><h2>Transactional build</h2><p>Current packed input, compiler refusal, accepted-output preservation, corrected publication, and deleted-owner cleanup.</p></div><code>pnpm check:v1-running-example</code></section>
      <section class="evidence-row"><div><span class="status-chip status-success">Executable</span><h2>Optional browser navigation</h2><p>Three-engine native fallback, history, focus, scroll, cancellation, and recovery qualification.</p></div><code>pnpm check:v2-history-focus-scroll</code></section>
      <section class="evidence-row"><div><span class="status-chip status-success">Executable</span><h2>Optional form enhancement</h2><p>Current packed package, exact GET controls, protected HTTPS actions, validation, pending state, refusal, no-JavaScript fallback, and non-repeating recovery in three browsers.</p></div><code>pnpm check:v2-form-submission</code></section>
    </div>
    <aside class="evidence-boundary"><strong>Evidence boundary</strong><p>These commands inspect normalized framework records. This page does not expose those private records or pretend a static build check ran during your request.</p></aside>
  </div>
);

export default page;
