Action ordering recovery: selected and unsafe traversal cancellation repaired committed truth through GET, and an inherited GET-form push rollback retained the same recovery owner; no path repeated the mutation.
