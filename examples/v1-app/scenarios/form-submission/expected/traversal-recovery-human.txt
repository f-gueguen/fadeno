Action ordering recovery: selected and unsafe traversal cancellation repaired committed truth through GET. When the selected URL could not be repaired because the history write failed, the browser replaced it with current truth instead of starting private recovery against the wrong URL. An inherited GET-form push rollback retained the same recovery owner; no path repeated the mutation.
