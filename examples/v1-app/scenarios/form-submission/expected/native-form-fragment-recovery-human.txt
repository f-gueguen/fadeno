Action ordering recovery: a superseding native GET form serialized successful controls once after submit listeners, accepted an image submitter from the browser-owned entry list, retained later `formdata` edits, and ignored a later programmatic serialization. It refused an unobservable cross-document result and unsafe destinations, preserved native fragment history (including a same-hash activation), selected a fresh current-truth document, and did not repeat POST.
