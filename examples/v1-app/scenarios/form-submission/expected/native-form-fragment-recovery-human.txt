Action ordering recovery: a superseding native GET form serialized successful controls once, preserved native fragment history (including a same-hash activation), selected a fresh current-truth document, and did not repeat POST.
