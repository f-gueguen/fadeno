Action ordering recovery: native activation stayed in the document, so Fadeno recovered committed current truth without repeating POST; this includes a trusted link cancelled before runtime handling, an initially eligible link cancelled by a later listener, and a link initially owned by another browsing context. Modified-primary and middle-button activations kept their browser-owned destination while the opener recovered. Final link destination, cancellation, browsing context, and privacy attributes were read after document listeners. A propagation-stopped late fragment change is not staged a second time, removed privacy protection followed final cancellation, and unchanged same-document or cross-document protected links stayed entirely browser-owned without a forced request.
