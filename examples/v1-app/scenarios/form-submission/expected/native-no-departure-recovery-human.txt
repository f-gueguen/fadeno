Action ordering recovery: native activation stayed in the document, so Fadeno recovered committed current truth without repeating POST; a same-document fragment retained its selected destination through a fresh native document, while a referrer-policy-protected fragment stayed entirely browser-owned without a forced request.
