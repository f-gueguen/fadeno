Action ordering recovery: submit propagation stopped before window finalization, reached it after a later listener cancelled the native default, or arrived at the runtime already cancelled during capture; committed current truth recovered once per cancelled case, while an unprevented stopped submit retained its final fragment destination and one native successful-control serialization. A `dialog` submission recovered without departure, and a late method change from `dialog` to GET used the final method and native destination without repeating POST.
