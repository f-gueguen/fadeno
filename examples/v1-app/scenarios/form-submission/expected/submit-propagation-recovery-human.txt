Action ordering recovery: submit propagation stopped before window finalization, reached it after a later listener cancelled the native default, or arrived at the runtime already cancelled during capture; committed current truth recovered once per cancelled case. Final privacy and form-data state selected the fragment, late unload cancellation was observed after every listener, a slow ineligible POST stayed browser-owned, `dialog` used its final method, and no POST was repeated.
