Action ordering recovery: submit propagation either stopped before window finalization or reached it after a later listener cancelled the native default; committed current truth recovered once per case, no native form data was serialized, and POST was not repeated.
