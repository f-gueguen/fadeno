Action ordering recovery: submit propagation stopped before window finalization, reached it after a later listener cancelled the native default, or arrived at the runtime already cancelled during capture; committed current truth recovered once per cancelled case. A stopped same-context form was refused without serialization, final browser-created form-data state selected safe fragment recovery, and an observed browser-owned object was never reconstructed when later routing became ineligible; the browser retained ownership of its completion or cancellation outcome. Late `onbeforeunload` cancellation was observed after every listener. An unobservable same-context POST and an ineligible link were refused before sending requests, so slow or no-document responses could not race committed-truth recovery. `dialog` used its final method, and no POST was repeated.
