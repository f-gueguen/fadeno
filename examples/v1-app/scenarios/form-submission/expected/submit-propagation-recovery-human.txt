Action ordering recovery: submit propagation stopped before window finalization, reached it after a later listener cancelled the native default, or arrived at the runtime already cancelled during capture; committed current truth recovered once per case, no native form data was serialized, and POST was not repeated.
