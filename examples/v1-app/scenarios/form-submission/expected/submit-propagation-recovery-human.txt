Action ordering recovery: submit propagation stopped before window finalization, reached it after a later listener cancelled the native default, or arrived at the runtime already cancelled during capture; committed current truth recovered once per cancelled case. Final privacy and browser-created form-data state selected the fragment without duplicate serialization, late `onbeforeunload` cancellation was observed after every listener, a slow ineligible POST stayed browser-owned, and a native no-content response recovered after retaining the current document. `dialog` used its final method, and no POST was repeated.
