import type { Page } from "fadeno-framework-internal";

const page: Page = () => (
  <section class="hero-card">
    <h1>CSS boundary</h1>
    <p>The application stylesheet owns this class.</p>
  </section>
);

export default page;
