# Extraction fixtures

The private K0-05 corpus locks five accepted and ten rejected TypeScript source
inputs through `inventory.golden.json`. The projection binds exact paths,
content hashes, triggers, classifications, logical module roles, and edges.

These fixtures are experiment inputs, not accepted public extraction syntax.
