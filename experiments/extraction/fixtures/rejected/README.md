# Rejected extraction fixtures

The locked rejection classes cover server secrets/modules, opaque capabilities,
class instances, cycles, dynamic imports, ambient environment switching,
unsupported async lifetime, oversized capture, and non-determinism. K0-05 will
execute one independently sensed server-boundary refusal control; K0-06 owns
full-corpus analyzer qualification.
